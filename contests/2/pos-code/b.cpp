/*
vector<string> A(H);

for(int i=0; i<H; i++)
    cin >> A[i];

vector<string> shift(int r(horizontal), int s(vertical))
        vector<string> res(H)

        for(int i=0. i<H; i++)
            for(int  j=0; j<W; i++)
                res[(i+r)%H][(j+s)%W] = A[i][j];


auto solve(const vector<string> A, B)

    for(int r=0; r<H; r++)
        for(int s=0; s<w; s++)
            if(shift(r,s, H, W, A) == B)
                return 'yes';
    return no;
*/